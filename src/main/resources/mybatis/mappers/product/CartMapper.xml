<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzasik.product.dao.CartDAO">
	<resultMap type="cartVO" id="cartResult">
		<result property="user_id" column="user_id" />
		<result property="pro_code" column="pro_code" />
		<result property="quantity" column="quantity" />
	</resultMap>
	
	<sql id="cartColumns">
		user_id,
		pro_code,
		quantity
	</sql>

	<!-- 장바구니 목록 -->
	<select id="selectCartList" parameterType="productVO" resultMap="productResult">
	SELECT *
	FROM product_cart
	WHERE user_id = #{user_id}
	</select>
	
	<!-- 장바구니 상품 추가 -->
	<insert id="insertCart" parameterType="java.util.Map">
	INSERT INTO product_cart (user_id, pro_code, quantity)
	VALUES (#{user_id}, #{pro_code}, #{quantity})
	</insert>
	
	<!-- 장바구니 상품 삭제 -->
	<delete id="deleteCart">
	DELETE
	FROM product_cart
	WHERE user_id = #{user_id} AND pro_code = #{pro_code}
	</delete>
</mapper>